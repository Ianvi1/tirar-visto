export type TitleBlockProps = {
  title: string;
  imageSrc: string;
  imageAlt: string;
  imageClasses: string;
  titleClasses?: string;
  textAlign: "left" | "right";
  italic?: boolean;
  relative?: boolean;
};
